node_modules
.next
out
.DS_Store
*.log
.env
.env.local
.env.*.local
.vercel
.turbo
.cache
coverage
dist
build
tmp
*.tsbuildinfo
next-env.d.ts

# Locally-installed agent skills (e.g. via `npx skills add ...`).
# These are tooling for the editor agent, not project source.
.agents/
skills-lock.json
